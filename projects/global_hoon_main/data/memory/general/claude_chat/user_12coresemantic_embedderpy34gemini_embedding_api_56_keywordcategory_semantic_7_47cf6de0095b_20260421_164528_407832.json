{
  "key": "user 12coresemantic_embedderpy34Gemini Embedding API를 활용한 시맨틱 유사도 엔진56스킬 선택 시 keywordcategory 점수에 semantic 유사도를 추가하여7의미 ",
  "value": "[user] 1\t\"\"\" 2\tcore/semantic_embedder.py 3\t========================= 4\tGemini Embedding API를 활용한 시맨틱 유사도 엔진. 5\t 6\t스킬 선택 시 keyword/category 점수에 semantic 유사도를 추가하여 7\t의미 기반 매칭 정확도를 높임. API 미사용 시 graceful fallback. 8\t\"\"\" 9\t 10\timport hashlib 11\timport json 12\ti...",
  "category": "claude_chat",
  "agent_id": "general",
  "memory_scope": "global",
  "source": "claude_session_bridge",
  "global_user_key": "hoon_main",
  "created_at": "2026-04-21T16:45:28.408408+00:00",
  "updated_at": "2026-04-21T16:45:28.408408+00:00",
  "details": {
    "text": "[user] 1\t\"\"\"\n2\tcore/semantic_embedder.py\n3\t=========================\n4\tGemini Embedding API를 활용한 시맨틱 유사도 엔진.\n5\t\n6\t스킬 선택 시 keyword/category 점수에 semantic 유사도를 추가하여\n7\t의미 기반 매칭 정확도를 높임. API 미사용 시 graceful fallback.\n8\t\"\"\"\n9\t\n10\timport hashlib\n11\timport json\n12\timport math\n13\timport os\n14\timport threading\n15\tfrom collections import OrderedDict\n16\tfrom typing import Dict, List, Optional\n17\t\n18\tfrom core.skill_metadata import SkillMetadata\n19\t\n20\tEMBEDDING_MODEL = \"models/gemini-embedding-001\"\n21\tEMBEDDING_DIM = 768\n22\tCACHE_DIR = \".system_generated/cache\"\n23\tCACHE_FILE = \"skill_embeddings.json\"\n24\tQUERY_CACHE_MAX = 100\n25\tMAX_SKILL_EMBEDDINGS = 10_000\n26\t\n27\t\n28\tclass SemanticEmbedder:\n29\t    \"\"\"Gemini 임베딩 기반 시맨틱 유사도 계산기.\"\"\"\n30\t\n31\t    def __init__(self):\n32\t        self._client = None\n33\t        self._is_available = False\n34\t        self._skill_embeddings: Dict[str, dict] = {}  # {skill_id: {\"hash\": str, \"embedding\": list}}\n35\t        self._query_cache: OrderedDict[str, List[float]] = OrderedDict()\n36\t        self._lock = threading.Lock()\n37\t\n38\t        self._try_init_api()\n39\t        self._load_disk_cache()\n40\t\n41\t    def _try_init_api(self):\n42\t        \"\"\"Gemini API 초기화 시도. 실패 시 is_available=False.\"\"\"\n43\t        try:\n44\t            api_key = os.getenv(\"GOOGLE_API_KEY\")\n45\t            if not api_key:\n46\t                return\n47\t            from google import genai\n48\t            self._client = genai.Client(api_key=api_key)\n49\t            self._is_available = True\n50\t        except Exception:\n51\t            self._is_available = False\n52\t\n53\t    @property\n54\t    def is_available(self) -> bool:\n55\t        return self._is_available\n56\t\n57\t    def precompute_skill_embeddings(self, skills: Dict[str, SkillMetadata]):\n58\t        \"\"\"변경된 스킬만 임베딩 재계산. API 불가 시 스킵.\"\"\"\n59\t        if not self._is_available:\n60\t            return\n61\t\n62\t        texts_to_embed = {}\n63\t        for skill_id, skill in skills.items():\n64\t            text = self._build_skill_text(skill)\n65\t            text_hash = hashlib.md5(text.encode()).hexdigest()[:16]\n66\t\n67\t            cached = self._skill_embeddings.get(skill_id)\n68\t            if cached and cached.get(\"hash\") == text_hash:\n69\t                continue\n70\t            texts_to_embed[skill_id] = (text, text_hash)\n71\t\n72\t        if not texts_to_embed:\n73\t            # 현재 스킬 목록에 없는 stale 항목 제거\n74\t            stale = set(self._skill_embeddings) - set(skills)\n75\t            if stale:\n76\t                for k in stale:\n77\t                    self._skill_embeddings.pop(k, None)\n78\t                self._save_disk_cache()\n79\t            return\n80\t\n81\t        try:\n82\t            texts = [t for t, _ in texts_to_embed.values()]\n83\t            embeddings = self._embed_documents(texts)\n84\t            if embeddings and len(embeddings) == len(texts):\n85\t                for (skill_id, (_, text_hash)), emb in zip(texts_to_embed.items(), embeddings):\n86\t                    self._skill_embeddings[skill_id] = {\"hash\": text_hash, \"embedding\": emb}\n87\t                # stale 항목 제거\n88\t                stale = set(self._skill_embeddings) - set(skills)\n89\t                for k in stale:\n90\t                    self._skill_embeddings.pop(k, None)\n91\t                # 크기 상한 초과 시 가장 오래된 항목부터 제거\n92\t                if len(self._skill_embeddings) > MAX_SKILL_EMBEDDINGS:\n93\t                    excess = len(self._skill_embeddings) - MAX_SKILL_EMBEDDINGS\n94\t                    for k in list(self._skill_embeddings.keys())[:excess]:\n95\t                        self._skill_embeddings.pop(k, None)\n96\t                self._save_disk_cache()\n97\t        except Exception:\n98\t            pass\n99\t\n100\t    def compute_similarity(self, task_input: str, skill: SkillMetadata) -> float:\n[result-id: r15]",
    "event_timestamp": "2026-04-08T16:33:27.275Z",
    "session_file": "C:/Users/HOME/.claude/projects/D--warkSpaces-agent-factory/fe021434-50a4-492e-ad4e-be201b3e00a1/subagents/agent-ae2384a81ebd95ea3.jsonl",
    "session_line": 53,
    "provider": "claude"
  }
}