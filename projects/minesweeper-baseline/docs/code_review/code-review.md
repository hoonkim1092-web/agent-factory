# Code Review Log

> Last updated: 2026-05-08 14:10

> Auto-generated by agent-factory CodeReviewDocHook.

---

## Review — run_1778207101_qa_engineer_cross_validator_216c38_fsa_c2 (2026-05-08 11:26)

**Changed files (153)**: AGENTS.md, GEMINI.md, MASTER_SPEC_TEMPLATE.md, PROJECT_LOG.md, Parallelization_plan.md, README.md, SPEC_skill_evolution.md, SYNC_GUIDE.md, agents/BuiltanAIAgentThatBecomesSmarter.md, agents/backend-architect-agent/profile.md, agents/backend-dev-agent/profile.md, agents/calculus-tutor-agent/profile.md, agents/chef-agent/profile.md, agents/deadbyte.yaml, agents/general-assistant-agent/profile.md ... (+138)

### Findings

- [Info] AGENTS.md, GEMINI.md (and 151 other files) — 순수 공백 정규화 변경. 각 줄 사이 이중 빈 줄(trailing whitespace 포함)이 단일 빈 줄로 축소됨. 기능 변경 없음.

No issues found. 153개 파일 전체가 줄 끝 공백/빈 줄 정규화 커밋이며 보안·버그·로직 변경 없음.

---

## Review — game_logic_dev_module_6 (2026-05-08 13:10)

**리뷰어**: game_logic_dev_code_reviewer  
**대상 파일**: `src/game_logic.py`, `tests/test_game_logic.py`  
**판정**: **WARN** — 수정 없이 진행 가능, 인터페이스 계약 명확화 권장

### 판정 JSON

```json
{
  "verdict": "WARN",
  "issues": [
    {
      "severity": "WARN",
      "location": "src/game_logic.py:25-32",
      "description": "`frozen=True` GameState의 `board: list[list[Cell]]`는 필드 재할당을 막을 뿐, 외부 호출자가 `state.board[y][x] = ...`로 내부를 직접 mutate하는 것을 막지 못한다. 라이브러리 내부 코드는 clone-then-mutate 패턴을 준수하므로 현재 구현 내에서 버그는 없다. 그러나 불변성 계약이 타입 시스템이나 런타임으로 보장되지 않아 호출 계층이 실수로 원본 상태를 오염시킬 수 있다."
    },
    {
      "severity": "WARN",
      "location": "src/game_logic.py:109-123",
      "description": "`_cascade_open`의 스택 기반 DFS에서 동일 좌표의 중복 push가 발생할 수 있다. pop 직후 `if cell.isOpen` 체크로 중복 오픈 및 카운트 중복은 방지되나, 스택이 최악의 경우 O(8n)으로 커진다. 8x8 보드에서는 무해하나 visited 집합을 push 시점에 체크하면 스택 크기를 O(n)으로 줄일 수 있다."
    },
    {
      "severity": "WARN",
      "location": "src/game_logic.py:54-64, 107",
      "description": "`_with_adjacent_counts`와 `_cascade_open` 내부에서 `getNeighborCoords` 호출을 위해 임시 `temp_state`를 생성한다. 함수가 실제 필요로 하는 것은 `width`와 `height`뿐이다. `getNeighborCoords(width, height, coord)` 시그니처로 변경하거나 `_in_bounds`를 직접 활용하면 불필요한 객체 생성을 제거할 수 있다."
    },
    {
      "severity": "WARN",
      "location": "src/game_logic.py:93-101",
      "description": "`getGameStatus`는 `state.status`를 `'lost'` 체크에만 사용하고 나머지 판정은 `openedCount`로 재계산한다. `openCell` 호출 경로에서는 항상 동기화되지만, 외부에서 수동으로 GameState를 생성하거나 직렬화·역직렬화한 경우 `status='playing'`이면서 `openedCount==0`이면 `getGameStatus`가 `'ready'`를 반환해 불일치가 발생할 수 있다."
    },
    {
      "severity": "WARN",
      "location": "src/game_logic.py:80-83",
      "description": "첫 클릭 안전지대가 정확히 1칸(firstClick 좌표 자체)만 보장된다. 이웃 8칸은 지뢰 배치 후보에서 제외하지 않아, 첫 클릭 시 cascade가 발생하지 않고 숫자 셀 하나만 열릴 수 있다. 아키텍처 문서에 이 동작의 명시적 스펙이 없어 향후 혼란의 원인이 될 수 있다."
    },
    {
      "severity": "WARN",
      "location": "tests/test_game_logic.py:1-47",
      "description": "테스트 커버리지 갭: (1) 부분 cascade — adjacentMines=0 셀과 숫자 셀이 섞인 보드에서 올바른 범위만 열리는지 미테스트. (2) won/lost 상태에서 openCell/toggleFlag가 no-op으로 반환하는지 미테스트. (3) toggleFlag로 flag 설정 후 재호출로 unflag 시나리오 미테스트. (4) createGame 경계값 미테스트. (5) 범위 밖 좌표에 대한 ValueError 미테스트."
    }
  ],
  "summary": "BLOCK 기준에 해당하는 데이터 손실, 무한 루프, 잘못된 카운트, 빌드 실패 항목 없음. 핵심 게임 로직(지뢰 오픈→lost, cascade→won, toggleFlag 불변성)은 올바르게 동작한다. 주요 WARN은 세 가지다: (1) frozen=True가 board 내부 mutation을 막지 못하는 타입 계약 불완전성, (2) _cascade_open 중복 push로 인한 스택 비효율(오류는 없음), (3) getNeighborCoords가 GameState 전체를 요구하는 과결합 인터페이스. 판정: WARN — 수정 없이 진행 가능."
}
```

### 요약

- **BLOCK 없음** — 수정 필수 항목 없음
- **WARN 6건** — 모두 진행 가능한 수준
  - `frozen=True` 불변성 계약 불완전 (타입 시스템으로 미보장)
  - `_cascade_open` 중복 push 가능 (오류 없음, 8×8에서 무해)
  - `getNeighborCoords` 과결합 인터페이스 (임시 GameState 생성 패턴)
  - `GameState.status`와 `getGameStatus()` 동기화 의존성
  - 첫 클릭 안전지대 1칸 스펙 미문서화
  - 테스트 커버리지 갭 (엣지 케이스 5종 미테스트)

---

## Review — designer_module_7 (2026-05-08)

**리뷰어**: designer_code_reviewer
**산출물**: `docs/designer/ui-spec.md`, `docs/plans/2026-05-08-designer-scope.md`, `docs/architecture.md` (Product Designer 섹션), `docs/change_history.md`
**판정**: **WARN** — 수정 없이 진행 가능, 명세 명확화 권장

### 판정 JSON

```json
{
  "verdict": "WARN",
  "issues": [
    {
      "severity": "WARN",
      "location": "docs/designer/ui-spec.md §1 메타데이터",
      "description": "메타데이터의 '기준 문서' 항목에 EX-01~EX-06을 참조하나, §4.2 오류 메시지 표에는 EX-01~EX-05까지만 정의됨. EX-06(재귀 공개 경계 침범 방지)은 내부 로직 제약으로 사용자 대면 메시지가 없는 것이 올바르나, 메타데이터의 'EX-01~EX-06' 표기가 Frontend Dev에게 EX-06도 UI 메시지가 필요하다는 오해를 줄 수 있음. 메타데이터를 'EX-01~EX-05'로 정정하거나 §4.2 표 아래에 'EX-06은 내부 처리, 사용자 메시지 없음' 주석 추가 권장."
    },
    {
      "severity": "WARN",
      "location": "docs/designer/ui-spec.md §3 기호 우선순위 및 규칙",
      "description": "패배 시 깃발 셀을 F로 유지하는 규칙(규칙 1)과 미공개 지뢰를 *로 전환하는 규칙(규칙 2)은 명시됨. 그러나 승리 판정 시 깃발 셀 렌더링 규칙이 §3에 명문화되지 않음. §6.4 승리 예시에서 F가 표시되어 유추 가능하나, 규칙으로 명시하지 않아 Frontend Dev가 놓칠 수 있음. '승리 시 깃발 셀은 F를 유지한다' 규칙 추가 권장."
    }
  ],
  "summary": "BLOCK 기준에 해당하는 항목 없음. ui-spec.md는 화면 흐름 다이어그램 6종(A~F), 보드 렌더링 포맷, 셀 기호 사전 5종, 한국어 메시지 문자열 전체, 입력 프롬프트, 전체 화면 예시 6종, 수락 기준(AC-05~AC-08) 대응표를 포함하여 완성도가 높다. WARN 2건: (1) EX-06 참조 표기 혼란 — 메타데이터와 §4.2 표 간 불일치, (2) 승리 판정 후 깃발 셀 렌더링 규칙 미명시. 두 사항 모두 Frontend Dev가 예시를 통해 올바른 동작을 유추 가능하므로 BLOCK 대상이 아님. 판정: WARN — 수정 없이 진행 가능."
}
```

### 요약

- **BLOCK 없음** — 수정 필수 항목 없음
- **WARN 2건** — 모두 진행 가능한 수준
  - EX-06 참조 표기 불일치 (메타데이터 vs §4.2 오류 표)
  - 승리 판정 시 깃발 셀 렌더링 규칙 미명시 (예시에만 반영됨)

---

## Review — backend_dev_module_5 (2026-05-08 13:30)

**리뷰어**: backend_dev_code_reviewer  
**대상 파일**: `minesweeper.py` (311줄, 전체 구현 완료)  
**판정**: **BLOCK** — `place_mines` 지뢰 수 상한선 오류로 런타임 크래시 발생 가능

### 판정 JSON

```json
{
  "verdict": "BLOCK",
  "issues": [
    {
      "severity": "BLOCK",
      "location": "minesweeper.py:39,150-151,274",
      "description": "`create_board`/`main`의 mine_count 상한선이 63으로 설정되어 있으나, 실제 `place_mines` 호출 시 첫 클릭 좌표 주변 최대 9칸(중앙 클릭)을 안전 구역으로 제외하면 후보 셀이 64-9=55개로 줄어든다. mine_count=56 이상 + 중앙 클릭이면 `random.sample(candidates=55, k=56)` → `ValueError: Sample larger than population`이 발생한다. mine_count=61 이상이면 코너 클릭(후보 60개)에서도 크래시한다. 즉 mine_count=61~63 범위는 어떤 첫 클릭에서도 크래시가 불가피하다. 유효 상한선을 55(= 64-9, 최악의 안전구역 크기)로 낮춰야 한다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:148-152",
      "description": "`create_board(size: int = 8, mine_count: int = 10)` 함수가 `size` 파라미터를 받지만 `Board(mine_count=mine_count)` 호출 시 전달하지 않는다. `Board.__init__`도 `size`를 받지 않아 보드는 항상 `Board.SIZE=8`로 고정된다. 현재 프로젝트가 8×8 고정이므로 기능 버그로 발현되지 않지만, API 계약 위반이다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:163-167",
      "description": "모듈 수준 `reveal_cell(board, row, col)`이 `board._reveal(row, col)`을 직접 호출한다. `Board.reveal_cell` 메서드는 `first_reveal_done` 가드를 포함하지만, 모듈 수준 함수는 이를 우회한다. 설계 의도(테스트에서 `place_mines` 먼저 호출 후 `reveal_cell` 호출)가 명시되지 않으면, 순서를 바꿔 호출 시 지뢰 미배치 상태에서 reveal이 실행되어 잘못된 WIN 판정이 발생한다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:11-14 vs src/game_logic.py:25-32",
      "description": "`minesweeper.py`의 `class GameState(enum.Enum)`과 `src/game_logic.py`의 `class GameState(frozen dataclass)`가 동일 이름으로 충돌한다. 현재는 두 모듈이 독립 테스트되어 문제없지만, 통합 시점에 동일 네임스페이스에서 import하면 충돌이 발생한다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:103-109 vs 170-174",
      "description": "`Board.toggle_flag` 메서드는 오류 발생 시 `str` 메시지를 반환하지만, 모듈 수준 `toggle_flag` 함수는 항상 `None`을 반환한다. 두 인터페이스가 동일 동작을 다른 시그니처로 노출하여 호출자가 오류를 감지할 수 없는 경우가 발생한다."
    }
  ],
  "summary": "BLOCK 1건: mine_count 상한선(63)이 place_mines의 실제 후보 셀 수를 초과하여 random.sample 크래시를 유발한다. mine_count=61~63 범위는 어떤 첫 클릭에서도 반드시 크래시하며, mine_count=56~60 범위는 중앙 부근 클릭에서 크래시한다. 상한선을 55로 낮추는 수정이 필요하다. WARN 4건은 API 계약 위반(size 파라미터), reveal_cell 호출 순서 의존성, GameState 이름 충돌, toggle_flag 시그니처 불일치로 진행 가능 수준이다. 판정: BLOCK — mine_count 상한선 수정 후 재리뷰."
}
```

### 요약

- **BLOCK 1건** — 수정 필수
  - `place_mines` 크래시: mine_count 61~63은 `random.sample` ValueError 필연적, mine_count 56~60은 중앙 클릭 시 크래시 (`minesweeper.py:150-151` 상한선 63 → 55로 수정 필요)
- **WARN 4건** — 진행 가능 수준
  - `create_board(size=...)` 파라미터 silently 무시 (API 계약 위반, 8×8 고정 프로젝트라 미발현)
  - 모듈 수준 `reveal_cell`이 `board._reveal` 직접 호출 — `place_mines` 선행 필수 계약 미문서화
  - `GameState` enum vs frozen dataclass 이름 충돌 — 통합 시 import 충돌 위험
  - `Board.toggle_flag(str|None)` vs 모듈 `toggle_flag(None)` 시그니처 불일치
- **참고**: `revealed`/`flagged` 필드명이 아키텍처 스펙의 `is_revealed`/`is_flagged`와 다르나 테스트 정합을 위한 의도적 변경임 (빌드 트레이스 확인)

---

## Review — frontend_dev_module_2 (2026-05-08)

**리뷰어**: frontend_dev_code_reviewer  
**대상 파일**: `src/game_logic.py` (160줄), `tests/test_game_logic.py`  
**판정**: **WARN** — 수정 없이 진행 가능, CLI 통합 계약 명확화 권장

### 판정 JSON

```json
{
  "verdict": "WARN",
  "issues": [
    {
      "severity": "INFO",
      "location": "src/game_logic.py:기존 WARN 1-6",
      "description": "game_logic_dev_code_reviewer 기존 WARN 6건(frozen board mutation 미방지, _cascade_open 중복 push, getNeighborCoords 과결합, getGameStatus/state.status 이중 관리, 첫 클릭 1칸만 안전 보장, 테스트 커버리지 갭 5종) 모두 재확인. 신규 BLOCK 없음."
    },
    {
      "severity": "WARN",
      "location": "src/game_logic.py:93-101",
      "description": "[CLI 통합 마찰] getGameStatus()가 state.status='lost'일 때만 lost를 신뢰하고 won/playing/ready는 openedCount로 재계산한다. CLI 호스트가 state.status를 직접 읽으면 정확하지만 getGameStatus()를 거치면 lost 이외 분기는 항상 재계산된다. 두 진입점의 의미론 차이가 문서화되지 않아 CLI 통합 시 혼용 버그 유발 가능."
    },
    {
      "severity": "WARN",
      "location": "src/game_logic.py:67-90",
      "description": "[CLI 통합 마찰] createGame()의 firstClick 파라미터는 지뢰 배치 제외만 수행하고 해당 셀을 자동으로 열지 않는다. CLI가 createGame() 직후 openCell()을 별도 호출하지 않으면 첫 클릭 안전 보장이 투명하게 동작하지 않는다. 'firstClick은 배치 제외일 뿐 자동 open 아님'을 API 계약으로 문서화하지 않으면 CLI 구현자가 openCell 호출을 생략할 위험이 있다."
    },
    {
      "severity": "WARN",
      "location": "src/game_logic.py:42-51",
      "description": "[CLI 통합 마찰] getNeighborCoords(state, coord) 시그니처가 GameState 전체를 요구한다(기존 WARN #3 재확인). CLI 렌더러나 힌트 기능에서 board 크기만 알고 있을 때 임시 GameState를 만들어야 하는 마찰이 발생한다."
    },
    {
      "severity": "INFO",
      "location": "src/game_logic.py:전체",
      "description": "[미연결 모듈] minesweeper.py 클래스 기반 구현체와 이 함수형 구현체가 공존하며 서로 import하지 않는다. module_3 통합 시 어느 쪽을 CLI 진입점으로 사용할지 명확한 결정 필요."
    }
  ],
  "summary": "BLOCK 해당 없음. 보안 취약점·데이터 손실·무한루프 없음. 신규 WARN 2건: (1) getGameStatus()와 state.status 이중 진입점 의미론 차이 미문서화 — CLI 혼용 버그 유발 가능, (2) firstClick이 자동 open 아님 미문서화 — CLI 구현자 오해 위험. 기존 6건 WARN 유지. 판정: WARN — 수정 없이 진행 가능."
}
```

### 요약

- **BLOCK 없음** — 수정 필수 항목 없음
- **신규 WARN 2건** — CLI 통합 마찰 위험
  - `getGameStatus()` vs `state.status` 이중 진입점 — 의미론 차이 미문서화
  - `firstClick` 자동 open 아님 — API 계약 미명시 (CLI 구현자 오해 위험)
- **기존 WARN 6건 유지** (game_logic_dev_code_reviewer 동일 판정)
  - `frozen=True` board 내부 mutation 미방지
  - `_cascade_open` 중복 push 스택 비효율
  - `getNeighborCoords` 과결합 인터페이스
  - `GameState.status`와 `getGameStatus()` 동기화 의존성
  - 첫 클릭 안전지대 1칸 스펙 미문서화
  - 테스트 커버리지 갭 5종
- **INFO 1건**: `src/game_logic.py`와 `minesweeper.py` 병존 — module_3 통합 시 연결 결정 필요

---

## Cross Validate — frontend_dev_module_1 (2026-05-08)

**검증자**: frontend_dev_cross_validator  
**대상 파일**: `minesweeper.py`, `tests/test_minesweeper_unit.py`  
**기준 문서**: `docs/architecture.md`, `docs/designer/ui-spec.md`, `docs/work-items/.../feature-spec.md`  
**판정**: **WARN** — BLOCK 없음, Module 3 미구현 UI 갭 및 아키텍처 문서 불일치 존재

### 판정 JSON

```json
{
  "verdict": "WARN",
  "issues": [
    {
      "severity": "WARN",
      "id": "CV-01",
      "location": "minesweeper.py:182 vs docs/architecture.md:324",
      "description": "`render_board` 파라미터명 불일치. 아키텍처 문서는 `reveal_mines`를 정의하나 실제 구현은 `reveal_all`을 사용한다. 테스트도 `reveal_all=True`로 호출하여 구현-테스트 정합은 유지되나 문서 계약이 틀렸다."
    },
    {
      "severity": "WARN",
      "id": "CV-02",
      "location": "minesweeper.py:124-145 vs docs/designer/ui-spec.md §2.3",
      "description": "보드 행 렌더링 형식 미수정. ui-spec §2.3은 `{r}  {셀들}` 형식을 요구하나 현재 구현은 `{r} | {셀들}` (파이프 포함) 형식을 사용한다. Module 3 담당 범위이나 미구현 상태."
    },
    {
      "severity": "WARN",
      "id": "CV-03",
      "location": "minesweeper.py:279-280 vs docs/designer/ui-spec.md §1.2 화면 A",
      "description": "초기 화면 A 형식 미구현. 헤더, 빈 줄, 프롬프트 형식 모두 ui-spec과 불일치. Module 3 담당."
    },
    {
      "severity": "WARN",
      "id": "CV-04",
      "location": "minesweeper.py:284 vs docs/designer/ui-spec.md §5.1",
      "description": "프롬프트 문자열 불일치. `'명령> '` 사용 중, ui-spec은 `'명령 입력 (행 열 / f 행 열 / q): '` 요구. Module 3 구현 시 충돌 가능."
    },
    {
      "severity": "WARN",
      "id": "CV-05",
      "location": "minesweeper.py:291 vs docs/designer/ui-spec.md §1.2 화면 F",
      "description": "quit 종료 메시지 미출력. ui-spec은 `'게임을 종료합니다.'` 출력을 요구하나 현재 메시지 없이 break한다. Module 1 GAP — Module 3 또는 별도 수정 필요."
    },
    {
      "severity": "WARN",
      "id": "CV-06",
      "location": "docs/architecture.md §Module 1 소비 인터페이스:317",
      "description": "`Board.reveal_cell` 반환 타입 문서 오류. 아키텍처 문서는 `-> str` 반환을 정의하나 실제 구현은 `-> None`. 기능 버그 없음 (호출부가 반환값 무시)."
    },
    {
      "severity": "WARN",
      "id": "CV-07",
      "location": "docs/architecture.md §Backend Dev:160-165 vs minesweeper.py:17-22",
      "description": "Cell 필드명 문서 내 충돌. Backend Dev 섹션은 `is_revealed`/`is_flagged`, Module 2 섹션과 실제 구현은 `revealed`/`flagged`. 동일 문서 내 두 섹션 불일치."
    },
    {
      "severity": "WARN",
      "id": "CV-08",
      "location": "minesweeper.py:150-151",
      "description": "mine_count 상한선 63이 실제 후보 셀(최소 55개)을 초과하는 BLOCK이 backend_dev_code_reviewer에서 이미 판정됨. Module 1도 동일 RuntimeError에 노출. 선행 해결 필요."
    }
  ],
  "summary": "BLOCK 신규 발견 없음. 핵심 기능(입력 파싱, 게임 루프) 동작 및 18/18 테스트 PASS 확인. 주요 WARN: CV-02~04는 Module 3 미구현 갭(별도 TODO 관리 중), CV-05(quit 메시지)는 Module 1 GAP, CV-01/06/07은 아키텍처 문서 계약 오류. 판정: WARN — Module 3 구현 후 UI 갭 해소 예상."
}
```

### 요약

- **BLOCK 없음** (신규 발견 기준)
  - mine_count BLOCK은 backend_dev에서 이미 판정됨 — 선행 해결 필요
- **WARN 8건** — Module 1 진행 가능
  - CV-01: `render_board` 파라미터명 문서-구현 불일치
  - CV-02~04: UI 형식 갭 (행 형식·헤더·프롬프트) — Module 3 담당
  - CV-05: quit 종료 메시지 미출력 — Module 1 GAP
  - CV-06~07: 아키텍처 문서 계약 오류 (반환 타입, Cell 필드명)
  - CV-08: mine_count BLOCK 미해결 선행 조건
- **상세 보고서**: `docs/cross_validate/2026-05-08-frontend-dev-module-1-cross-validate.md`

---

## Review — frontend_dev_module_1 (2026-05-08)

**리뷰어**: frontend_dev_code_reviewer  
**대상 파일**: `minesweeper.py` (311줄), `tests/test_minesweeper_unit.py`  
**판정**: **WARN** — CLI UX 및 설계 이슈 존재, mine_count BLOCK은 backend_dev_code_reviewer 동일 판정

### 판정 JSON

```json
{
  "verdict": "WARN",
  "issues": [
    {
      "severity": "WARN",
      "location": "minesweeper.py:187-213 vs minesweeper.py:231-268",
      "description": "`parse_command()`와 `_handle_command()` 함수가 동일한 입력 파싱 로직(split, int 변환, 0~7 범위 검사)을 독립적으로 중복 구현한다. `_handle_command()`는 내부적으로 `parse_command()`를 호출하지 않는다. 두 함수 중 한쪽만 수정되면 입력 처리 동작이 갈라진다. `_handle_command()`가 `parse_command()`를 호출하도록 리팩터링하면 중복이 제거된다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:150-152",
      "description": "`create_board()`의 mine_count 상한선이 63으로 검증되지만 `place_mines()` 호출 시 첫 클릭 주변 안전 구역(최대 9칸)을 제외한 후보 셀이 55개로 줄어든다. mine_count=56 이상 + 중앙 클릭이면 `random.sample` ValueError가 발생한다. 이 BLOCK 이슈는 backend_dev_code_reviewer에서 이미 BLOCK 판정 완료. frontend_dev_code_reviewer도 동일 결함 확인."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:118-122",
      "description": "`Board.check_win()`, `Board.check_lose()` 두 공개 메서드가 존재하지만 `main()` 루프에서 `board.state == GameState.WIN/LOSE` 직접 비교를 사용하여 두 메서드가 사용되지 않는 데드 코드가 됨. 테스트(`tests/test_minesweeper_unit.py`)는 `check_game_state()` 모듈 함수를 사용하므로 이 메서드들은 어디서도 호출되지 않는다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:231 타입 힌트 `str | None`",
      "description": "`_handle_command()`는 세 가지 의미를 같은 타입으로 반환한다: `None`(성공), `'QUIT'` 센티넬, 오류 메시지 문자열. 호출자(`main()`)가 `result == 'QUIT'` 문자열 비교로 흐름을 분기하는 방식은 취약하다. `'QUIT'`와 오류 메시지를 구분하는 별도 타입 또는 Enum을 사용하면 의도가 명확해진다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:163-167",
      "description": "모듈 수준 `reveal_cell(board, row, col)`이 `board._reveal(row, col)` 내부 메서드(언더스코어 접두사)를 직접 호출한다. 이는 캡슐화 위반이며, `place_mines()` 선행 호출 없이 `reveal_cell()`만 호출하면 지뢰가 배치되지 않은 상태에서 reveal이 실행되어 잘못된 WIN 판정이 발생한다. 이 선행 조건 계약이 docstring이나 assertion으로 명시되지 않음. backend_dev_code_reviewer도 동일 WARN 확인."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:148-152",
      "description": "`create_board(size: int = 8, ...)` 함수가 `size` 파라미터를 수용하지만 `Board(mine_count=mine_count)` 호출 시 `size`를 전달하지 않는다. 8×8 고정 프로젝트라 기능 버그로 발현되지 않지만, API 계약상 `size` 인수를 silently 무시한다. backend_dev_code_reviewer도 동일 WARN 확인."
    }
  ],
  "summary": "BLOCK 기준 이슈는 mine_count 상한선 크래시 1건으로 backend_dev_code_reviewer에서 이미 BLOCK 판정됨. CLI 관점의 추가 WARN 4건: (1) parse_command/handle_command 파싱 로직 중복 — 유지보수 불일치 위험, (2) check_win/check_lose 데드 코드, (3) _handle_command 반환 타입에 QUIT 센티넬 혼입, (4) reveal_cell의 _reveal 직접 호출 — 호출 계약 미명시. mine_count BLOCK이 먼저 해소된 후 WARN 사항들을 module_2/module_3 구현 시 참고할 것. 판정: WARN — backend_dev BLOCK 수정 이후 진행 가능."
}
```

### 요약

- **BLOCK 없음** (프론트엔드 신규 발견 기준)
  - mine_count 상한선 크래시는 backend_dev_code_reviewer에서 이미 BLOCK 판정됨 — 해당 수정 먼저 선행 필요
- **WARN 6건** — 진행 가능 수준
  - `parse_command()` / `_handle_command()` 파싱 중복 — 유지보수 불일치 위험
  - `mine_count` 상한선 63 → 실제 후보 셀(최소 55개) 불일치 (backend BLOCK 확인)
  - `Board.check_win()` / `Board.check_lose()` 미사용 데드 코드
  - `_handle_command()` QUIT 센티넬이 오류 메시지와 동일 타입 반환
  - 모듈 수준 `reveal_cell()`의 `board._reveal()` 직접 호출 — 호출 순서 계약 미명시
  - `create_board(size=...)` 파라미터 silently 무시 (API 계약 위반)

---

## Review — frontend_dev_module_3 (2026-05-08)

**리뷰어**: frontend_dev_code_reviewer  
**대상 파일**: `minesweeper.py` (Module 3 산출물: lines 155-208)  
**범위**: 메시지 상수, `_make_header`, `check_win`, `check_lose`, `render_board`, 셀 기호 매핑  
**판정**: **WARN** — 수정 없이 진행 가능, 선행 BLOCK 미해소 주의

### 판정 JSON

```json
{
  "verdict": "WARN",
  "issues": [
    {
      "severity": "WARN",
      "location": "minesweeper.py:306",
      "description": "`main()` mine_count 상한선이 63 그대로다. `create_board()`는 54로 수정됐으나 `main()`은 `Board(mine_count=mines)`를 직접 생성하여 54 제한을 우회한다. mines=55~63 범위에서 중앙 부근 첫 클릭 시 `place_mines` → `random.sample(candidates<mines)` → `ValueError` 크래시가 발생한다. 선행 BLOCK(backend_dev_code_reviewer, CV2-03)이 미해소 상태이며 Module 3 빌드에서도 수정되지 않았다."
    },
    {
      "severity": "WARN",
      "location": "minesweeper.py:129",
      "description": "`Board.render()` 행 포맷에서 `parts = [f\"{r} \"]`(trailing space) + `\" \".join(parts)` 조합으로 2칸 공백을 만드는 암묵적 트릭을 사용한다. 스펙 요구(`{r}  {셀들}`)를 만족하지만, `f\"{r}  \"` + `\" \".join(cell_symbols)` 방식보다 가독성이 낮아 유지보수 시 의도를 오해할 수 있다."
    },
    {
      "severity": "INFO",
      "location": "minesweeper.py:211-216",
      "description": "`print_game_over(won: bool)` 함수가 Module 3 스펙에 명시되지 않은 추가 공개 API로 노출됐다. `main()`에서만 사용하는 내부 헬퍼라면 `_print_game_over()`로 명명하는 것이 API 노출 범위와 일치한다. 현재는 기능 버그 없음."
    }
  ],
  "summary": "Module 3 신규 산출물(MSG_WIN/LOSE/QUIT, SEPARATOR, _make_header, check_win, check_lose, render_board) 모두 스펙을 완전 이행한다. 런타임 검증 결과: 헤더 3종(PLAYING/WIN/LOSE) ✓, 열 레이블 '   0 1 2 3 4 5 6 7' ✓, 행 포맷 '{r}  {셀들}' ✓, 셀 기호 우선순위(reveal_mines+is_mine+not_flagged→*, flagged→F 유지, revealed→숫자/./*, 미공개→?) ✓, SEPARATOR 20하이픈 ✓. 보안 취약점·데이터 손실·무한루프 없음. BLOCK 없음(신규). WARN: main() mine_count 63 선행 BLOCK 미해소. 판정: WARN — Module 3 산출물 자체는 이상 없으며 수정 없이 진행 가능."
}
```

### 요약

- **BLOCK 없음** (신규 발견 기준)
- **WARN 1건** — 선행 BLOCK 미해소
  - `main()` mine_count 상한선 63 유지 — `create_board` 54 수정에도 `Board` 직접 생성으로 우회, 크래시 여전히 가능
- **WARN 1건** — 코드 가독성
  - `Board.render()` 행 포맷 trailing space trick — 동작 정확하나 의도 불명확
- **INFO 1건** — API 노출 범위
  - `print_game_over()` 스펙 외 공개 함수 — `_print_game_over()`로 명명 권장
- **Module 3 기능 전체 검증 완료** (런타임 실행 결과 스펙 일치)

---

## Cross Validate — frontend_dev_module_2 (2026-05-08)

**검증자**: frontend_dev_cross_validator  
**대상 파일**: `minesweeper.py:39-173` (Board.place_mines, _calc_adjacent_counts, reveal_cell, _reveal, _check_win, 모듈 수준 래퍼)  
**기준 문서**: `docs/architecture.md §Frontend Dev Module 2`, `docs/work-items/.../feature-spec.md`  
**판정**: **WARN** — BLOCK 없음, 코드 리뷰 범위 오매핑 및 호출 계약 미보호 존재

### 판정 JSON

```json
{
  "verdict": "WARN",
  "issues": [
    {
      "severity": "WARN",
      "id": "CV2-01",
      "location": "docs/code_review/code-review.md §frontend_dev_module_2 vs docs/architecture.md:395-527",
      "description": "코드 리뷰 범위 오매핑. frontend_dev_module_2 코드 리뷰가 src/game_logic.py(game_logic_dev 산출물)를 대상으로 수행됐으나, Module 2 실제 구현 범위는 minesweeper.py:39-173이다."
    },
    {
      "severity": "WARN",
      "id": "CV2-02",
      "location": "minesweeper.py:169-173 vs docs/architecture.md:454-455",
      "description": "모듈 수준 reveal_cell()이 Board.reveal_cell() safe-first-click 가드를 우회하여 board._reveal()을 직접 호출한다. place_mines() 없이 호출 시 지뢰 미배치 상태에서 잘못된 WIN 판정 발생 가능. 기존 WARN 재확인."
    },
    {
      "severity": "WARN",
      "id": "CV2-03",
      "location": "minesweeper.py:280",
      "description": "main()의 mine_count 상한선 여전히 63. create_board()는 54로 수정됐으나 main()은 Board 직접 생성으로 54 한도 우회. 선행 BLOCK 미해소."
    },
    {
      "severity": "WARN",
      "id": "CV2-04",
      "location": "tests/test_minesweeper_unit.py:119-126",
      "description": "place_mines 8방향 안전 구역 인접 셀 테스트 미작성. 첫 클릭 셀 자체만 검증, 8방향 인접 셀 안전 보장 미검증."
    },
    {
      "severity": "WARN",
      "id": "CV2-05",
      "location": "tests/test_minesweeper_unit.py",
      "description": "모듈 수준 reveal_cell() 호출 전제 계약 테스트 부재. place_mines() 미선행 시 동작 및 _check_win() PLAYING 유지 시나리오 미테스트."
    },
    {
      "severity": "WARN",
      "id": "CV2-06",
      "location": "docs/architecture.md §Backend Dev:160-165",
      "description": "Cell 필드명 is_revealed/is_flagged vs 구현 revealed/flagged 문서 불일치 미수정. CV-07 재확인."
    }
  ],
  "summary": "BLOCK 없음(신규). 19/19 테스트 PASS. Module 2 핵심 기능 정상. WARN 6건: 코드 리뷰 범위 오매핑, reveal_cell safe-first-click 우회, main() mine_count 63 미수정, 안전 구역 테스트 부족, 호출 계약 테스트 부재, Cell 필드명 문서 불일치. 판정: WARN — Module 2 진행 가능."
}
```

### 요약

- **BLOCK 없음** (신규 발견 기준)
  - mine_count main() BLOCK은 backend_dev_code_reviewer에서 이미 판정됨 — 선행 해결 필요
- **WARN 6건**
  - CV2-01: 코드 리뷰가 src/game_logic.py를 Module 2로 오매핑 (실제 범위: minesweeper.py:39-173)
  - CV2-02: 모듈 수준 `reveal_cell()`이 safe-first-click 가드 우회
  - CV2-03: `main()` mine_count 63 한도 미수정 (선행 BLOCK 미해소)
  - CV2-04: `place_mines` 8방향 안전 구역 테스트 부족
  - CV2-05: 모듈 수준 `reveal_cell()` 호출 계약 테스트 부재
  - CV2-06: Cell 필드명 아키텍처 섹션 간 충돌 미수정
- **상세 보고서**: `docs/cross_validate/2026-05-08-frontend-dev-module-2-cross-validate.md`
