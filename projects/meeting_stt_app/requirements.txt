# 실행 가능한 PySide6 회의록 STT 데스크톱 앱 — 런타임 의존성 (Windows 전용)
# scope §2.1 확정 스택. 버전은 하한만 고정(상호 호환 범위).

PySide6>=6.6            # Qt6 GUI, QThread, Signal/Slot
faster-whisper>=1.0.0  # 로컬 STT (CTranslate2 백엔드, 내장 vad_filter)
PyAudioWPatch>=0.2.12  # WASAPI loopback + 마이크 캡처 (PyAudio 포크)
numpy>=1.24            # 16kHz mono 리샘플·믹싱·RMS

# 패키징(배포 빌드 시에만 필요)
PyInstaller>=6.0       # onedir 패키징 (ctranslate2 DLL·모델 번들)

# 참고:
# - WAV 저장은 표준 라이브러리 wave 모듈을 사용하므로 soundfile/scipy 불필요.
# - 외부 webrtcvad 미사용 (faster-whisper 내장 vad_filter로 대체).
