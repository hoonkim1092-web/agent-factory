# 프로젝트 TODO

- [ ] Backend Dev: 동행복권 API 데이터 수집 모듈 범위와 인터페이스를 정의하고 구현 순서를 고정한다.
- [ ] Backend Dev: 동행복권 API 데이터 수집 모듈을(를) 구현한다. 기능을 작은 슬라이스로 나눠 구현한다.
- [ ] Backend Dev: 동행복권 API 데이터 수집 모듈 결과를 검증하고 다음 작업자가 이어받을 handoff 메모를 남긴다.
- [ ] Backend Dev: SQLite 기반 당첨번호 캐시 저장소 범위와 인터페이스를 정의하고 구현 순서를 고정한다.
- [ ] Backend Dev: SQLite 기반 당첨번호 캐시 저장소을(를) 구현한다. 기능을 작은 슬라이스로 나눠 구현한다.
- [ ] Backend Dev: SQLite 기반 당첨번호 캐시 저장소 결과를 검증하고 다음 작업자가 이어받을 handoff 메모를 남긴다.
- [ ] Backend Dev: 번호별 출현빈도·동시출현·구간 통계 분석 엔진 범위와 인터페이스를 정의하고 구현 순서를 고정한다.
- [ ] Backend Dev: 번호별 출현빈도·동시출현·구간 통계 분석 엔진을(를) 구현한다. 기능을 작은 슬라이스로 나눠 구현한다.
- [ ] Backend Dev: 번호별 출현빈도·동시출현·구간 통계 분석 엔진 결과를 검증하고 다음 작업자가 이어받을 handoff 메모를 남긴다.
- [ ] Backend Dev: 가중 랜덤 기반 5조합 추천 생성기 범위와 인터페이스를 정의하고 구현 순서를 고정한다.
- [ ] Backend Dev: 가중 랜덤 기반 5조합 추천 생성기을(를) 구현한다. 기능을 작은 슬라이스로 나눠 구현한다.
- [ ] Backend Dev: 가중 랜덤 기반 5조합 추천 생성기 결과를 검증하고 다음 작업자가 이어받을 handoff 메모를 남긴다.
- [ ] Backend Dev: 터미널 통계 요약 출력 포맷터 범위와 인터페이스를 정의하고 구현 순서를 고정한다.
- [ ] Backend Dev: 터미널 통계 요약 출력 포맷터을(를) 구현한다. 기능을 작은 슬라이스로 나눠 구현한다.
- [ ] Backend Dev: 터미널 통계 요약 출력 포맷터 결과를 검증하고 다음 작업자가 이어받을 handoff 메모를 남긴다.
- [ ] Backend Dev: CLI 엔트리포인트 범위와 인터페이스를 정의하고 구현 순서를 고정한다.
- [ ] Backend Dev: CLI 엔트리포인트 (fetch / analyze / recommend 서브커맨드)을(를) 구현한다. 기능을 작은 슬라이스로 나눠 구현한다.
- [ ] Backend Dev: CLI 엔트리포인트 결과를 검증하고 다음 작업자가 이어받을 handoff 메모를 남긴다.
- [ ] QA Engineer: QA Engineer 검증 범위와 인터페이스를 정의하고 구현 순서를 고정한다.
- [ ] QA Engineer: 핵심 플로우와 회귀 시나리오를 검증한다. 기능을 작은 슬라이스로 나눠 구현한다.
- [ ] QA Engineer: QA Engineer 검증 결과를 검증하고 다음 작업자가 이어받을 handoff 메모를 남긴다.
- [ ] 아키텍처나 워크플로가 바뀌면 `docs/architecture.md`를 현재 설계에 맞게 갱신한다.
- [ ] 설계 변경마다 날짜, 요약, 이유, 영향 파일, 후속 작업을 `docs/change_history.md`에 추가한다.
- [ ] 생성하거나 수정하는 모든 문서는 운영체제 언어 코드 `ko-KR`에 맞는 언어인 한국어로 작성한다.
