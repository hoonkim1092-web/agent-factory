# Code Review: cli

> Source: core/providers/cli.py
> Date: 2026-05-26 01:14
> Type: code
> Providers: critic=codex
> Mode: single-provider (code critic only)
> Trigger: unknown

---

## Code Critic Review

### Verdict: PASS

### T3 Advisory

t3_required: yes

This touches runtime CLI behavior: user-facing subprocess status output and git-context decoding inside prompt construction.

### Findings

No correctness, security, or AF-specific blocking findings found in this diff.

Clean evidence:

1. [No issue] Progress output change is display-only
   - File: `core/providers/cli.py:248`
   - Code: `print(f"  [{provider_id}] running... "`
   - Evidence: This only changes human-readable progress text. It does not alter timeout math, subprocess invocation, result parsing, or provider selection.

2. [No issue] Git subprocess calls remain shell-safe
   - File: `core/providers/cli.py:605`
   - Code: `subprocess.run(["git", "rev-parse", "--short", "HEAD"], ... encoding="utf-8", errors="replace", timeout=3, check=False)`
   - Evidence: The command is still passed as a list with `shell=False` default. The change adds deterministic decoding and replacement rather than introducing shell interpolation.

3. [No issue] CLI result semantics unchanged
   - File: `core/providers/cli.py:904`
   - Code: `if ok:`
   - Evidence: Success/failure branching and returned `result` fields are unchanged; only printed status text changed at lines 906 and 909.

### Comparison with Known Issues

- The change does not introduce known critical patterns from `docs/code_review/code-review.md`: no non-atomic write, no `shell=True`, no new `thread.join(timeout)`, no shared mutable cache, and no new `core/*.py` module requiring `af.spec`.
- The touched subprocess calls improve a known Windows/encoding class of failure by specifying `encoding="utf-8", errors="replace"` for git context collection.
- Existing targeted tests pass: `pytest -q tests/test_cli_providers.py` → `37 passed`. Pytest emitted unrelated existing `UnicodeDecodeError` warnings from background subprocess reader threads, but the suite passed.

### Positive Observations

- The git context collection remains bounded with `timeout=3`, so prompt enrichment cannot hang the provider path.
- The ASCII status output is safer for Windows consoles with legacy encodings.