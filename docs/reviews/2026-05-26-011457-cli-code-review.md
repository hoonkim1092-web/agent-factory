# Code Review: cli

> Source: core/providers/cli.py
> Date: 2026-05-26 01:14
> Type: code
> Providers: critic=codex
> Mode: single-provider (code critic only)
> Trigger: unknown

---

## Code Critic Review

### Verdict: PASS

### T3 Advisory

t3_required: yes

This touches provider CLI behavior and subprocess decoding, so Tier 3 is required.

### Findings

1. [No Issue] Git subprocess calls remain shell-safe
   - File: `core/providers/cli.py:605`
   - Code: `subprocess.run(["git", "rev-parse", "--short", "HEAD"], cwd=repo_root, capture_output=True, text=True, encoding="utf-8", errors="replace", timeout=3, check=False)`
   - Issue: No shell injection regression. The command remains argv-list based with default `shell=False`; `repo_root` is discovered locally from `.git`.
   - Suggestion: No fix required.

2. [No Issue] UTF-8 decoding is appropriate for prompt metadata
   - File: `core/providers/cli.py:620`
   - Code: `subprocess.run(["git", "log", "-5", "--oneline"], cwd=repo_root, capture_output=True, text=True, encoding="utf-8", errors="replace", timeout=3, check=False)`
   - Issue: `errors="replace"` can lose exact byte fidelity, but this path only injects best-effort git context into prompts. It is not used for parsing, auth, policy, or file selection.
   - Suggestion: No fix required. A regression test with non-local-codepage commit text would strengthen this.

3. [No Issue] ASCII CLI status output is behaviorally safe
   - File: `core/providers/cli.py:782`
   - Code: `f"  [{display_name}] CLI start (timeout: {effective_timeout}s)"`
   - Issue: Callers read the returned result dict, not these human progress strings. I checked production callers in `agent_runner`, `builder`, `cross_verification`, `dogfood`, and `control_plane_llm`; they inspect `ok`, `text`, `reason`, `command`, etc.
   - Suggestion: No fix required.

### Comparison with Known Issues

- This change partially addresses the Windows/encoding class of issues by removing non-ASCII progress output and forcing UTF-8 decoding for git context subprocesses.
- It does not introduce known bad patterns from `code-review.md`: no `shell=True`, no direct file write, no new global cache, no thread join, and no new provider-specific API-key assumption.
- Existing but not introduced here: `_collect_git_context()` still has broad `except Exception: pass` at `core/providers/cli.py:617`, `626`, and `635`, matching the known silent-fallback pattern. Since git context is optional prompt enrichment, I would not block this diff on it.

### Positive Observations

- The encoding change was applied consistently to all four git-context subprocess calls.
- The status messages keep timeout/failure details while avoiding Windows console decoding failures.

Verification: `pytest tests\test_cli_providers.py -q` passed: 37 passed. Pytest still reported 3 existing `UnicodeDecodeError` warnings from subprocess reader threads under cp949, but they were not failures and are not from the changed git-context subprocess calls.