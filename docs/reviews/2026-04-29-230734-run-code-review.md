# Code Review: run

> Source: scripts/run.py
> Date: 2026-04-29 23:07
> Type: code
> Providers: critic=claude, cross=codex, judge=claude
> Mode: cross-review (code, 2 providers)
> Trigger: unknown

---

## Final Code Review

### Verdict: WARN

High 4건 (Critical 없음). 머지 전 Finding #1/#2 수정 강권, #3/#4는 단기 후속 조치 권장.

---

### Aggregated Findings (8 total, 7 ACCEPT / 1 REJECT)

#### 1. [ACCEPT] [High] `cwd=_ROOT` + venv/workspace 우회 — hook workspace 항상 AF 루트
- **Critic**: `run.py`의 `cwd=_ROOT`와 `cli_hook_bridge.py`의 `--workspace` 부재로 `handle_hook_event`에 전달되는 workspace가 항상 AF 레포 루트.
- **Cross**: `.py` direct dispatch가 `hook_runner.py`의 `_find_venv_python()` / `_detect_workspace()`를 완전히 우회. `settings.local.template.json:46`의 `check_pending_review.py` 호출도 이 경로.
- **Judgment**: 양쪽이 동일 결함을 다른 레이어에서 확인. Critic은 인자 전달 누락, Cross는 venv 선택 우회. 동일 근본 원인(`run.py`의 직접 실행 경로). 이전 세션 cross-review에서도 ACCEPT됐으나 **이번 diff에서 미수정**.
- **Action Required**: `_ROOT` 계산 전 `caller_cwd = os.getcwd()` 캡처 → `cli_hook_bridge` 호출 시 `--workspace {caller_cwd}` 추가. 또는 Cross 제안대로 `.py` special case 제거 후 모든 target을 `hook_runner.py`로 위임.

#### 2. [ACCEPT] [High] `.py` direct dispatch에 timeout 없음 — 무한 대기 가능
- **Critic**: not flagged
- **Cross**: `hook_runner.py:423`은 `proc.wait(timeout=120)` 보호, `run.py`의 `.py` 경로는 이를 우회.
- **Judgment**: Cross 단독이나 증거가 명확 (`hook_runner.py:423` 라인 확인). `check_pending_review.py`가 블로킹 시 Claude Code 세션 전체가 멈출 수 있음.
- **Action Required**: `.py` branch에 `timeout=120` 추가 또는 `.py` special case 제거 후 `hook_runner.py` 위임.

#### 3. [ACCEPT] [High] `spec_from_file_location` None 반환 → `AttributeError` — 진단 불가 크래시
- **Critic**: `session_adapter.py` 미존재 시 `spec_from_file_location`이 `None` 반환 → `module_from_spec(None)` → `AttributeError` — 실제 원인(파일 없음) 메시지 없음.
- **Cross**: not flagged
- **Judgment**: Critic 단독이나 Python `importlib` 동작이 명확하므로 ACCEPT. hook이 아무 진단 없이 죽는 운영 영향.
- **Action Required**: `cli_hook_bridge.py:15` 직후 `if _spec is None: sys.exit(f"[cli_hook_bridge] session_adapter not found: {_sa_path}")` 추가.

#### 4. [ACCEPT] [High] `json.JSONDecodeError` 미처리 — hook 전체 크래시
- **Critic**: `raw`이 빈 값이 아닌 malformed JSON(타임아웃, partial write)일 때 unhandled exception → hook traceback 종료.
- **Cross**: not flagged
- **Judgment**: Critic 단독이나 Claude Code의 non-zero exit 처리 정책상 세션 차단 가능. `cli_hook_bridge.py:34` 한 줄 수정으로 해결.
- **Action Required**: `try: payload = json.loads(raw) if raw else {} except json.JSONDecodeError: payload = {}` 로 감싸기.

#### 5. [ACCEPT] [Medium] 모든 에러 경로 `return 0` — 실패가 성공으로 보고
- **Critic**: script 미존재, hook_runner 미존재 등 오류에서 exit 0 반환. review-gate같은 차단 hook의 안전장치 무력화.
- **Cross**: not flagged
- **Judgment**: Critic 단독. "no args"와 "unsafe target rejected"는 0이 맞으나 "파일 없음" 오류는 1이 맞음. 의미론적으로 명확.
- **Action Required**: `scripts/run.py:26,34`의 script/runner 미존재 경로를 `return 1`로 변경.

#### 6. [ACCEPT] [Medium] `--provider` choices에 `codex` 누락 — Codex 환경 즉시 실패
- **Critic**: `providers/registry.py`의 `CLI_PROVIDER_IDS`에 `codex_cli` 포함, `cli_hook_bridge.py:25`의 `choices=("claude", "gemini")`에 없음 → argparse validation error.
- **Cross**: not flagged
- **Judgment**: Critic 단독이나 registry 코드가 직접 증거. Codex 환경에서 hook 전체 동작 불가.
- **Action Required**: `choices=("claude", "gemini", "codex")` 또는 choices 제거 후 `handle_hook_event` 내 unknown provider 처리.

#### 7. [ACCEPT] [Medium] `handle_hook_event` 누락 시 AttributeError — 진단 없음
- **Critic**: `session_adapter.py`가 `handle_hook_event`를 export하지 않으면 AttributeError — 어떤 심볼이 없는지 메시지 없음.
- **Cross**: not flagged
- **Judgment**: Finding #3의 연장선. `try/except AttributeError`로 명확한 종료 메시지 가능.
- **Action Required**: `cli_hook_bridge.py:20` 주변에 `try/except AttributeError: sys.exit("[cli_hook_bridge] handle_hook_event not found in session_adapter")` 추가.

#### 8. [REJECT] [Low] stdin forwarding 단절
- **Critic**: not flagged
- **Cross**: 초기 우려 제기 후 자체 기각 — `subprocess.run`이 stdin 기본 상속, smoke run 검증 통과.
- **Judgment**: Cross reviewer가 실제 실행으로 반증. 기각.

---

### Summary Table

| # | Title | Severity | Verdict | Source |
|---|-------|----------|---------|--------|
| 1 | `cwd=_ROOT` + venv 우회 | High | ACCEPT | Both |
| 2 | `.py` dispatch timeout 없음 | High | ACCEPT | Cross |
| 3 | `spec_from_file_location` None → crash | High | ACCEPT | Critic |
| 4 | `json.JSONDecodeError` 미처리 | High | ACCEPT | Critic |
| 5 | 에러 경로 `return 0` | Medium | ACCEPT | Critic |
| 6 | `--provider` choices codex 누락 | Medium | ACCEPT | Critic |
| 7 | `handle_hook_event` AttributeError 진단 없음 | Medium | ACCEPT | Critic |
| 8 | stdin forwarding 단절 | Low | REJECT | Cross |

---

### Recommendations

- **즉시 (머지 차단 수준)**: Finding #1 — `run.py`의 `.py` branch에서 `caller_cwd` 캡처 후 `--workspace` 전달, 또는 `.py` special case 제거 후 `hook_runner.py` 위임 (Finding #2 timeout도 동시 해결)
- **단기 (이번 PR 포함 권장)**: Finding #3/#4 — `cli_hook_bridge.py`에 `_spec is None` 조기 종료 + `JSONDecodeError` try/except 각 1줄 추가
- **후속 (다음 PR)**: Finding #5 `return 1` 수정, Finding #6 choices 확장, Finding #7 AttributeError 래핑
- Finding #1은 **이전 세션 ACCEPT 판정 후 미수정** — 재발 방지를 위해 NEXT_STEPS.md에 명시 권장