# Code Review: __init__

> Source: core/research/__init__.py
> Date: 2026-05-07 17:27
> Type: code
> Providers: critic=claude, cross=codex, judge=claude
> Mode: cross-review (code, 3 providers)
> Trigger: unknown

---

## Final Code Review

### Verdict: WARN

BLOCK 기준(Critical)은 없으나, High 결함 1건 + Medium 결함 1건이 병존. 문서화된 리스크로 머지 가능하나 즉시 후속 커밋 권고.

---

### Aggregated Findings (3 total)

#### 1. [ACCEPT] [High] 커밋된 `__init__.py`가 미추적(untracked) 파일을 임포트
- **Critic**: "빈 `__init__.py`로 검토 불가" (diff 미제공으로 내용 미확인)
- **Cross**: "ACCEPT — `__init__.py:1-8`이 `work_spec`, `quality_contract`, `checklist_merger`를 임포트하지만 세 파일 모두 `git status`에서 `??`"
- **Judgment**: Cross 리뷰가 실제 파일 내용과 git 상태를 직접 확인. Critic은 diff 부재로 동일 이슈를 간접 지적. 이 상태로 레포를 클론하면 `import core.research` 시 `ModuleNotFoundError` 발생 — 커밋이 불완전함.
- **Action Required**: 같은 커밋(또는 즉시 다음 커밋)에 `core/research/work_spec.py`, `core/research/quality_contract.py`, `core/research/checklist_merger.py` 및 `packs/` 의존 파일을 포함시키거나, 해당 파일들이 준비될 때까지 `__init__.py`의 re-export를 제거.

---

#### 2. [ACCEPT] [Medium] `af.spec` hiddenimports에 `core.research` 패키지 미등록
- **Critic**: "신규 `core/research/` 패키지가 `af.spec` hiddenimports에 누락 시 frozen 빌드에서 `ModuleNotFoundError` 발생. §M9 패턴 반복 위험."
- **Cross**: "ACCEPT — `af.spec:61-63`에 `core.research_router`, `core.researcher`, `core.research_verifier`는 있으나 `core.research` 및 하위 모듈 없음."
- **Judgment**: 양쪽이 동일 파일의 동일 위치를 지목하며 증거 일치. 빌드 배포 시 `af.exe`가 임포트 실패할 확실한 경로.
- **Action Required**: `af.spec` hiddenimports에 아래를 추가:
  ```python
  "core.research",
  "core.research.work_spec",
  "core.research.quality_contract",
  "core.research.checklist_merger",
  ```
  또는 `collect_submodules('core.research')` 사용.

---

#### 3. [ACCEPT] [Low] `__init__.py` public API 노출 — 검토 완료, 이슈 없음
- **Critic**: "비어 있으면 `from core.research import Researcher` 불가, 일관성 깨짐."
- **Cross**: "REJECT — `rg` 결과 외부 production caller 없음. 65개 테스트 통과."
- **Judgment**: Cross 리뷰가 실제 re-export 코드와 테스트 결과로 반박. Critic의 우려는 diff 미제공 상태에서의 보수적 추정. 실질적 문제 없음. 기록용으로만 포함.
- **Action Required**: 없음.

---

### Summary Table

| # | Title | Severity | Verdict | Source |
|---|-------|----------|---------|--------|
| 1 | 미추적 sibling 모듈 임포트 | High | ACCEPT | Cross (Critic 간접) |
| 2 | af.spec hiddenimports 누락 | Medium | ACCEPT | Both |
| 3 | public API 노출 우려 | Low | ACCEPT(무효) | Critic only → 반박됨 |

---

### Recommendations

- **즉시(머지 전 또는 직후 커밋)**: `core/research/` 하위 3개 파일 + `packs/` 의존 파일을 git에 추가. Finding 1이 해소될 때까지 이 패키지는 클론 환경에서 broken 상태.
- **빌드 전 필수**: `af.spec` hiddenimports 업데이트 — CLAUDE.md "새 `core/*.py` 파일은 af.spec에 추가" 규칙과 기존 §M9 패턴에 따라 처리.
- **참고**: Cross 리뷰가 `65 passed` 결과를 제시했으나 이는 기존 테스트 스위트 기준이며 `core.research` 임포트 경로를 직접 커버하지 않을 가능성 있음 — 신규 패키지 스모크 테스트 추가 권고.