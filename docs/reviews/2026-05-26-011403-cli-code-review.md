# Code Review: cli

> Source: core/providers/cli.py
> Date: 2026-05-26 01:14
> Type: code
> Providers: critic=codex
> Mode: single-provider (code critic only)
> Trigger: unknown

---

## Code Critic Review

### Verdict: PASS

### T3 Advisory

t3_required: yes

This touches subprocess output decoding and CLI behavior, so Tier 3 review is required.

### Findings

1. [No Issue] Git commands remain shell-safe after encoding change
   - File: `core/providers/cli.py:605`
   - Code: `subprocess.run(["git", "rev-parse", "--short", "HEAD"], cwd=repo_root, capture_output=True, text=True, encoding="utf-8", errors="replace", timeout=3, check=False)`
   - Issue: No shell injection found. The command is still argv-list based with `shell=False` default, and `repo_root` comes from local `.git` discovery.
   - Suggestion: No fix required.

2. [No Issue] Windows decode hardening is correctly scoped
   - File: `core/providers/cli.py:620`
   - Code: `subprocess.run(["git", "log", "-5", "--oneline"], cwd=repo_root, capture_output=True, text=True, encoding="utf-8", errors="replace", timeout=3, check=False)`
   - Issue: The change avoids locale-dependent decoding failures for git output. `errors="replace"` can lose exact byte fidelity, but this context is prompt metadata, not a parser or security boundary.
   - Suggestion: No fix required. A focused regression test with non-CP949/non-default-locale commit text would strengthen coverage.

3. [No Issue] ASCII progress output avoids console encoding failures
   - File: `core/providers/cli.py:782`
   - Code: `f"  [{display_name}] CLI start (timeout: {effective_timeout}s)"`
   - Issue: Replacing emoji/Korean progress text with ASCII reduces Windows console/log encoding risk. This is behaviorally safe because callers consume the result dict, not these status strings.
   - Suggestion: No fix required.

### Comparison with Known Issues

- Does this change address known issues? Partially. It addresses the Windows/encoding class of CLI subprocess/logging problems by making git-context decoding explicit and progress output ASCII-only.
- Does it introduce known bad patterns? No new `shell=True`, file writes, thread joins, global caches, or provider-specific assumptions were introduced.
- Existing pattern still present but not introduced here: `_collect_git_context()` still uses broad `except Exception: pass` at `core/providers/cli.py:617`, `626`, and `635`. Since the changed behavior is best-effort prompt enrichment, I would not block this diff on it.

### Positive Observations

- `encoding="utf-8", errors="replace"` was applied consistently to all four git-context subprocess calls.
- The changed CLI messages preserve timeout/failure information while avoiding non-ASCII output that can break Windows subprocess readers.

Verification: `pytest tests/test_cli_providers.py -q` passed: 37 passed. Pytest reported 3 existing Windows `UnicodeDecodeError` warnings from subprocess reader threads elsewhere in the test run; not traced to the changed git-context calls.