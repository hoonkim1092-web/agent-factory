# Code Review: cli

> Source: core/providers/cli.py
> Date: 2026-05-26 01:15
> Type: code
> Providers: critic=codex
> Mode: single-provider (code critic only)
> Trigger: unknown

---

## Code Critic Review

### Verdict: PASS

### T3 Advisory

t3_required: yes

The change affects runtime CLI behavior and prompt context collection, not just docs/whitespace.

### Findings

No correctness, security, or AF compatibility findings in the provided diff.

Clean evidence:

1. `core/providers/cli.py:606`
   - Code: `encoding="utf-8", errors="replace"`
   - Why clean: Git subprocess output is now decoded deterministically and won’t crash on undecodable bytes.

2. `core/providers/cli.py:782`
   - Code: `f"  [{display_name}] CLI start (timeout: {effective_timeout}s)"`
   - Why clean: Console text changed only; no control flow, subprocess args, auth, provider selection, or result shape changed.

3. `core/providers/cli.py:909`
   - Code: `print(f"  [{display_name}] failed: {failure_reason} ({mins}m {secs}s)", flush=True)`
   - Why clean: Failure classification and returned `reason` are unchanged; only user-facing log text changed.

### Comparison with Known Issues

- The change does not introduce known critical patterns from `docs/code_review/code-review.md`: no non-atomic writes, no `shell=True`, no new cache, no new shared mutable state.
- The touched `_collect_git_context` area still has existing `except Exception: pass` silent fallback blocks, but this diff does not introduce them.
- Multi-provider behavior remains provider-neutral; the changes apply across CLI providers without assuming Claude/Gemini/Codex-specific semantics.

### Positive Observations

- Replacing localized/emoji progress output with ASCII improves Windows console and frozen-build log compatibility.
- Pinning Git subprocess decoding to UTF-8 with replacement directly reduces Unicode decode fragility in prompt context collection.

Validation: `pytest tests\test_cli_providers.py -q` passed: 37 passed, 1 existing UnicodeDecodeError warning from a background subprocess reader thread.