# Code Review: __init__

> Source: core/research/__init__.py
> Date: 2026-05-07 17:27
> Type: code
> Providers: critic=claude, cross=codex, judge=claude
> Mode: cross-review (code, 3 providers)
> Trigger: unknown

---

## Final Code Review

### Verdict: BLOCK

Two independent reviewers confirmed a frozen-build crash path. Combined with a broken extractor that silently fails 100% of calls, the code is not mergeable as-is.

---

### Aggregated Findings (7 total)

#### 1. [ACCEPT] [Critical] Frozen build: `_PACKS_DIR` uses `__file__` without frozen guard
- **Critic**: "`_PACKS_DIR = Path(__file__).parent / "packs"` — PyInstaller에서 `.pyc` archive 경로가 되어 `packs/*.yaml`이 존재하지 않음. 모든 팩 누락 → `QualityContractBuildError`"
- **Cross**: "`af.spec:27-31`에 `core/research/packs` 데이터 엔트리 없음. 빌드 후 팩 파일 부재로 contract degradation 또는 실패"
- **Judgment**: 양쪽이 동일 코드 경로(`quality_contract.py:66-67`)를 독립적으로 추적해 동일한 런타임 실패를 확인. `code-review.md §3.3 M9` 기존 패턴과 일치. af.spec 수정도 필요.
- **Action Required**:
  ```python
  # quality_contract.py:66-67
  import sys as _sys
  _BASE = Path(_sys._MEIPASS) if getattr(_sys, "frozen", False) else Path(__file__).parent
  _PACKS_DIR = _BASE / "packs"
  ```
  `af.spec` datas에 추가: `("core/research/packs", "core/research/packs")`

#### 2. [ACCEPT] [High] `WorkSpecExtractor.extract()` 항상 빈 WorkSpec 반환
- **Critic**: "`execute_requirement_prompt()`가 dict 반환 → `_extract_json(dict)` 호출 시 `TypeError` → bare `except Exception:` 이 매번 잡아 `WorkSpec()` 반환. 추출이 단 한 번도 성공하지 않음"
- **Cross**: not flagged
- **Judgment**: 단일 리뷰어지만 증거가 명확: dict를 str-expecting 함수에 전달하는 타입 불일치 + bare except로 무한 침묵. 기능적으로 완전 고장.
- **Action Required**:
  ```python
  # work_spec.py:61-63
  raw = execute_requirement_prompt(prompt)
  text = str(raw.get("text") or "")
  data = json.loads(_extract_json(text))
  ```

#### 3. [ACCEPT] [High] `core.research` 패키지가 `af.spec` hiddenimports 누락
- **Critic**: "`af.spec`에 `core.research_router` 등은 있으나 `core.research` 하위 패키지 4개 전부 누락. 프로즌 빌드에서 `ModuleNotFoundError` 발생"
- **Cross**: not flagged (하지만 Cross의 frozen build 지적과 같은 근본 원인)
- **Judgment**: 단일 리뷰어지만 `af.spec` diff에서 직접 확인 가능한 누락. 프로즌 빌드 첫 import에서 crash 확실.
- **Action Required**: `af.spec` hiddenimports에 추가:
  ```python
  'core.research',
  'core.research.work_spec',
  'core.research.quality_contract',
  'core.research.checklist_merger',
  ```

#### 4. [ACCEPT] [High] YAML 파싱 실패 시 silent fallback — 진단 정보 없음
- **Critic**: "`except Exception: return []` — 어느 팩 파일이, 왜 실패했는지 알 수 없음. `code-review.md §H3` 패턴 반복"
- **Cross**: not flagged
- **Judgment**: bare except + silent return은 기존 코드리뷰 블록 패턴과 동일. finding #2(extractor 고장)와 결합 시 데이터 전손 가능.
- **Action Required**: `logger.warning("_load_pack failed: %s — %s", path, exc)` before `return []`

#### 5. [ACCEPT] [Medium] Greedy JSON 정규식이 LLM 출력에서 과다 매칭
- **Critic**: "`r\"\\{[\\s\\S]*\\}\"` — greedy로 첫 `{`~마지막 `}` 전체 캡처. `code-review.md §H3` ISERedesigner JSON parse 패턴 재현"
- **Cross**: not flagged
- **Judgment**: 단일 리뷰어지만 greedy vs non-greedy 차이와 LLM 출력 특성으로 증거 충분.
- **Action Required**: `work_spec.py:79` → `r"\{[\s\S]*?\}"` (non-greedy)

#### 6. [ACCEPT] [Medium] `ChecklistMerger`에서 `id` 누락 아이템 silent discard
- **Critic**: "팩 YAML의 `id` 필드 오타(`"ID"` 등) → `id=""` → merger silently skip. 진단 없는 데이터 손실"
- **Cross**: not flagged
- **Judgment**: `_load_pack()`의 `str(entry.get("id") or "")` → `""` → skip 경로가 명확. Medium 유지.
- **Action Required**: `_load_pack()`에서 `entry.get("id")`가 falsy일 때 warning log 추가

#### 7. [ACCEPT] [Low] `core/research/__init__.py` public import 테스트 없음
- **Critic**: not flagged
- **Cross**: "`__all__` 심볼 import path 회귀 테스트 없음. 미래 re-export 변경 시 무감지"
- **Judgment**: Cross만 플래그했으나 근거 타당. 단, 기존 기능 동작에 영향 없어 Low로 유지.
- **Action Required**: smoke test 추가: `from core.research import WorkSpec, QualityContractBuilder, ChecklistMerger`

---

### Summary Table

| # | Title | Severity | Verdict | Source |
|---|-------|----------|---------|--------|
| 1 | Frozen build: `_PACKS_DIR` + spec datas 누락 | Critical | ACCEPT | Both |
| 2 | `WorkSpecExtractor` 항상 빈 WorkSpec 반환 | High | ACCEPT | Critic |
| 3 | `core.research` hiddenimports 누락 | High | ACCEPT | Critic |
| 4 | YAML 파싱 silent fallback | High | ACCEPT | Critic |
| 5 | Greedy JSON 정규식 | Medium | ACCEPT | Critic |
| 6 | `id` 누락 아이템 silent discard | Medium | ACCEPT | Critic |
| 7 | `__init__.py` 테스트 없음 | Low | ACCEPT | Cross |

---

### Recommendations

- **즉시 수정 (BLOCK 해제 필수)**: #1 frozen guard + spec datas, #2 WorkSpec 타입 불일치, #3 hiddenimports 추가
- **같은 PR에 포함 권장**: #4 YAML parse warning log (bare except 패턴 — code-review.md 반복 위반)
- **후속 PR 허용**: #5 greedy regex, #6 id discard log, #7 smoke test
- Finding #1, #2, #3은 `af.exe` 배포 빌드에서 모두 crash로 이어지는 독립적 경로 — 하나만 수정해서는 충분하지 않음