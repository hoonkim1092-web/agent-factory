# Code Review: __init__

> Source: core/research/__init__.py
> Date: 2026-05-07 17:26
> Type: code
> Providers: critic=claude, cross=codex, judge=claude
> Mode: cross-review (code, 3 providers)
> Trigger: unknown

---

## Final Code Review

### Verdict: WARN

High finding exists (frozen build will fail at runtime). Can merge with documented risk, but af.spec must be fixed before any frozen build is produced.

---

### Aggregated Findings (2 total)

#### 1. [ACCEPT] [High] af.spec missing both hiddenimport and data files for core.research

- **Critic**: "`core.research` modules not listed in `af.spec` hiddenimports — per project rule M9, missing entries cause silent runtime crashes in frozen builds."
- **Cross**: "`core/research/packs/` not listed in `af.spec` datas — `QualityContractBuilder.build()` loads `core/research/packs/base.yaml` at `quality_contract.py:84`; if the directory is absent, the call raises `QualityContractBuildError` at line 119. `af.spec:27-31` confirmed missing the entry."
- **Judgment**: Both reviewers independently identified the same root cause (af.spec not updated for the new subpackage), but from complementary angles — one found the module import gap, the other found the data file gap. Cross reviewer supplied line-level evidence (`quality_contract.py:84`, `quality_contract.py:119`, `af.spec:27-31`). Both findings are real; combined they represent a complete frozen-build failure path.
- **Action Required**:
  1. Add `'core.research'` (and any peer modules discovered via `Glob core/research/*.py`) to `af.spec` `hiddenimports`.
  2. Add `('core/research/packs', 'core/research/packs')` to `af.spec` `datas`.
  3. Add a smoke test: `QualityContractBuilder().build(WorkSpec())` returns at least one base checklist item (guards against silent empty-pack regressions).

---

#### 2. [ACCEPT] [Medium] Blueprint §0 not updated in same commit

- **Critic**: "CLAUDE.md requires new `core/*.py` files trigger a §0 quick-reference table update in the same commit. No Blueprint diff was present in the review input."
- **Cross**: Not flagged.
- **Judgment**: Single-reviewer finding, but the evidence is the project's own CLAUDE.md rule ("새 `.py` 파일 생성 → §0 빠른 참조 테이블 업데이트 필수, 같은 커밋"). The new `core/research/` subpackage with its modules clearly qualifies. Not a code correctness issue but a process violation that breaks the Blueprint-as-authoritative-reference invariant.
- **Action Required**: Add `core/research/__init__.py` (and any peer modules) to `Master_Blueprint.md` §0 quick-reference table. Include the Blueprint edit in the same commit as the code.

---

### Summary Table

| # | Title | Severity | Verdict | Source |
|---|-------|----------|---------|--------|
| 1 | af.spec missing hiddenimport + packs data | High | ACCEPT | Both |
| 2 | Blueprint §0 not updated | Medium | ACCEPT | Critic |

Rejected by Cross (no action needed): lazy LLM import concern, wildcard import caller concern — both verified clean.

---

### Recommendations

- Fix `af.spec` before the next frozen build: add `core.research` to `hiddenimports` and `core/research/packs` to `datas`.
- Update `Master_Blueprint.md` §0 in the same commit as the `core/research/` files.
- Add a single smoke test for `QualityContractBuilder().build()` to prevent silent data-file regressions in future deployments.